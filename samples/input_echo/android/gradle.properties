# Deliberately small: no AndroidX (the app carries no Java code at
# all), and the JVM args are the AGP default heap so the build behaves
# the same on the dev machine and a runner.
org.gradle.jvmargs=-Xmx2048m
